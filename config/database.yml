development:
  # Sqlite3 Settings
  adapter: sqlite3
  database: db/development.sqlite3
  timeout: 5000
  
  # Mysql Settings
  # adapter: mysql
  # encoding: utf8
  # database: test_development
  # username: root
  # password:

test:
  # Sqlite3 Settings
  adapter: sqlite3
  database: db/test.sqlite3
  timeout: 5000
  
  # Mysql Settings
  # adapter: mysql
  # encoding: utf8
  # database: test_development
  # username: root
  # password:

production:
  Mysql Settings
  adapter: mysql
  encoding: utf8
  database: gamble_track_prod
  username: root
  password:
